/**
 * dsh-layout-control — host placeholder.
 * All behavior lives in the browser half (`lib/client.js`). An empty apply
 * exists so the cordis.patch.yml loader row can mount.
 */
export function apply() {}
